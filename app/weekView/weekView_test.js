'use strict';

describe('myApp.weekView module', function() {

  beforeEach(module('myApp.weekView'));

  describe('Week controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('WeekCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});