'use strict';

describe('myApp.dayView module', function() {

  beforeEach(module('myApp.dayView'));

  describe('Day controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var scope = {};
      var dayCtrl = $controller('DayCtrl');
      expect(dayCtrl).toBeDefined();
    }));

  });
});